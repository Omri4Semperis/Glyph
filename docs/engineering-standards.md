## Coding Standards

- Methods must not start with the word "get_" as it's too generic and can lead to confusion. Instead, use more descriptive names that clearly indicate the purpose of the method.
- Variables must have 3 chars or more to them (except for loop counters).